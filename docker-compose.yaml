services:
  yanblog:
    build: .
    ports:
      - "3002:80"   # 前台
      - "3011:81"   # 后台
    volumes:
      - uploads_data:/app/uploads
      - ./config:/app/host-config:ro
    restart: always

volumes:
  uploads_data:
    driver: local
